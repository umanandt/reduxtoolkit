const StatusCode ={
    LOADING: 'loading',
    IDLE: 'idle',
    ERROR: 'error',
}

export default StatusCode;

// to minimize the possibilities of error 
// because we were writing status too many times
// so using this constant we can reduc the chance
